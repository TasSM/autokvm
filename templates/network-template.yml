network:
  version: 1
  config:
    - type: physical
      name: eth0
      subnets:
        - type: static
          address: {{IP_ADDRESS}}/{{PREFIX}}
          gateway: {{GATEWAY}}
          dns_nameservers:
            - {{DNS1}}
            - {{DNS2}}